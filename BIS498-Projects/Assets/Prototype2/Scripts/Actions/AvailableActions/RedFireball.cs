using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedFireball : Fireball
{
    // Start is called before the first frame update
    void Start()
    {
        actionName = "RedFireBall";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
